import { AppShell } from '../shared/app-shell/AppShell';

export default function TournamentAdminApp() {
	return (
		<AppShell
			title="Tournament Admin"
			description="Manage brackets, categories, and competition settings."
		/>
	);
}
