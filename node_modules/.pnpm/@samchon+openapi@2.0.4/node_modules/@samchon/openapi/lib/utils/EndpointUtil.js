"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EndpointUtil = void 0;
var NamingConvention_1 = require("./NamingConvention");
var EndpointUtil;
(function (EndpointUtil) {
    EndpointUtil.capitalize = function (str) {
        return str.length !== 0 ? str[0].toUpperCase() + str.slice(1).toLowerCase() : str;
    };
    EndpointUtil.pascal = function (path) {
        return EndpointUtil.splitWithNormalization(path)
            .filter(function (str) { return str[0] !== "{"; })
            .map(NamingConvention_1.NamingConvention.pascal)
            .join("");
    };
    EndpointUtil.splitWithNormalization = function (path) {
        return path
            .split("/")
            .map(function (str) { return EndpointUtil.normalize(str.trim()); })
            .filter(function (str) { return !!str.length; });
    };
    EndpointUtil.reJoinWithDecimalParameters = function (path) {
        return path
            .split("/")
            .filter(function (str) { return !!str.length; })
            .map(function (str) {
            return str[0] === "{" && str[str.length - 1] === "}"
                ? ":".concat(str.substring(1, str.length - 1))
                : str;
        })
            .join("/");
    };
    EndpointUtil.normalize = function (str) {
        str = str.split(".").join("_").split("-").join("_").trim();
        if (RESERVED.has(str))
            return "_".concat(str);
        else if (str.length !== 0 && "0" <= str[0] && str[0] <= "9")
            str = "_".concat(str);
        return str;
    };
    EndpointUtil.escapeDuplicate = function (keep) {
        return function (change) {
            return keep.includes(change) ? EndpointUtil.escapeDuplicate(keep)("_".concat(change)) : change;
        };
    };
})(EndpointUtil || (exports.EndpointUtil = EndpointUtil = {}));
var RESERVED = new Set([
    "break",
    "case",
    "catch",
    "class",
    "const",
    "continue",
    "debugger",
    "default",
    "delete",
    "do",
    "else",
    "enum",
    "export",
    "extends",
    "false",
    "finally",
    "for",
    "function",
    "if",
    "import",
    "in",
    "instanceof",
    "module",
    "new",
    "null",
    "package",
    "public",
    "private",
    "protected",
    "return",
    "super",
    "switch",
    "this",
    "throw",
    "true",
    "try",
    "typeof",
    "var",
    "void",
    "while",
    "with",
]);
